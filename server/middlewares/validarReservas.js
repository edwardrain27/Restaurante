const {request, response} = require('express');

/**
 * Método encargado de valir el número de reservas por día
 * @param {*} req 
 * @param {*} res 
 */
const validarFecha = async(req=request, res=response)=>{
    
}